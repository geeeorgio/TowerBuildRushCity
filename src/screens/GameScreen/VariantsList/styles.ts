import { StyleSheet } from 'react-native';

import { hp } from 'src/utils';

export const styles = StyleSheet.create({
  list: {
    width: '100%',
  },
  listContainer: {
    gap: hp(10),
  },
  listFooter: {
    height: hp(40),
  },
});
